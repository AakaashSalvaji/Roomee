# App Base

## Creating New App

1. Copy this Repo
1. Rename the app name and version in `package.json` and `app.json`
1. Delete `package-lock.json`
1. Run `npm i` to install packages
1. Copy `.env.example` as `.env` and insert configuration
1. Run the app with `npm run start`